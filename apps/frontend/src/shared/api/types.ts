export type TPagination = {
	page?: number | string;
	per_pages?: number | string;
};
export type TId = {
	id: number;
};
export type TIds = {
	ids: number[];
};
export type TSlug = {
	slug: string;
};
